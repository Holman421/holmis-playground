/**
 * Creates and returns the default debug object for the radar shader
 */
export function createRadarDebugObject() {
	return {
		// FBM movement settings
		fbmOffsetX: 0,
		fbmOffsetY: 0,
		fbmMoveSpeed: 0.5
	};
}
